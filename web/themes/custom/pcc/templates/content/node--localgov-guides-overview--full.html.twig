{#
/**
 * @file
 * Theme override for the localgov_guides_overview pages template.
 */
#}
{% set classes = ['lgd-guides', 'lgd-guides--overview', 'node', 'node--type-' ~ (node.bundle|clean_class), node.isPromoted() ? 'node--promoted', node.isSticky() ? 'node--sticky', not node.isPublished() ? 'node--unpublished', view_mode ? 'node--view-mode-' ~ (view_mode|clean_class)] %}

<article {{ attributes.addClass(classes).removeAttribute('role') }}>
  <div class="container">
    <div class="content">
      {{ title_prefix }}
      <h2>{{ guide_overview_title|default('Overview'|t) }}</h2>
      {{ title_suffix }}

      {% if content %}
        {{ content }}
      {% endif %}
    </div>
  </div>
</article>
